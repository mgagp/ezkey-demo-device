name: ezkey-demo-device

services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: ezkey-demo-device:local
    ports:
      - "3080:8083"
    environment:
      # Default: Exp1 public Auth API. Override in .env or the shell for a local stack.
      EZKEY_AUTH_API_URL: ${EZKEY_AUTH_API_URL:-https://exp1-auth-api.ezkey.org}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    volumes:
      - demo-device-data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-fsS", "http://127.0.0.1:8083/"]
      interval: 30s
      timeout: 3s
      retries: 3
      start_period: 40s

volumes:
  demo-device-data:
